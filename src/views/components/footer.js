import Component from '../common/component'

export default class Footer extends Component {
    setup () {
      this.state = {  }
    }

    template() {
        return `
            it's footer
        `
    }
}